groups:
  - name: sso-api
    rules:
      - alert: SsoApiDown
        expr: up{job="sso-api"} == 0
        for: 1m
        labels: { severity: critical }
        annotations: { summary: SSO API down }
